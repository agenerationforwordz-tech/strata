# STRATA - Self-Hosted AI Memory Server
# Copyright (c) 2026 A Generation Forwordz Foundation
#
# Core dependencies (required for both SQLite and PostgreSQL backends)
mcp>=1.0.0
fastembed>=0.2.0
numpy>=1.24.0
uvicorn>=0.30.0
starlette>=0.35.0

# PostgreSQL backend (only needed if STRATA_DB_BACKEND=postgresql)
# Comment these out if using SQLite only
psycopg2-binary>=2.9.0    # PostgreSQL adapter (binary wheel, no build tools needed)
pgvector>=0.3.0            # pgvector extension for similarity search in PostgreSQL
